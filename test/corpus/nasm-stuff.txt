================================================================================
dumb comments
================================================================================

; dumb comment
_start:     mov     rax, 2
            mov     rdi, path ; trailing comment
            xor     rsi, rsi

--------------------------------------------------------------------------------

(ERROR
  (ERROR
    (comment)
    (comment))
  (UNEXPECTED '\0'))

================================================================================
hard comments
================================================================================

_start:     mov     rax, 2      ; "open"
            mov     rdi, path   ; 
            xor     rsi, rsi    ; O_RDONLY

--------------------------------------------------------------------------------

(source_file
  (ERROR
    (comment)
    (comment))
  (toplevel_item
    (statement
      (directive
        (ins
          (MISSING ins_kw)))
      (comment))))
